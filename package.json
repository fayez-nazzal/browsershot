{
  "name": "browsershot",
  "version": "1.0.0",
  "description": "Capture a web page to a PNG or GIF via your installed Chrome (Playwright).",
  "type": "module",
  "bin": {
    "browsershot": "src/cli.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test"
  },
  "dependencies": {
    "playwright": "^1.61.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
