app: fixture-app
default: test-user
credentials:
  test-user:
    purpose: fixture test user
    email: test@example.com
    password: not-a-real-secret
    app_url: https://example.test/app
    signed_in_when:
      url_matches: "https://example.test/app/**"
